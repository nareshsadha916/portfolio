{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "description": "Student Portfolio Monorepo",
  "scripts": {
    "build": "npm install --prefix backend && cd frontend && npm install --include=dev && npm run build && cd ..",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
